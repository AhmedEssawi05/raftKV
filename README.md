# raftKV
A from-scratch Raft consensus implementation backing a replicated key-value store — leader election, log replication, and crash recovery via WAL, built and tested with zero dependencies. Includes a deterministic simulated-network test harness and a live 3-node failover demo over real HTTP.
